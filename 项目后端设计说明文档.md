# 私人助手项目

一个基于 FastAPI 和 SQLite 构建的私人AI助手，具有聊天，上传文件，调节模型参数，知识库管理等功能。

## 1-1 项目概述

这是一个AI助手系统后端API服务，采用异步Python框架FastAPI开发，
使用SQLite 作为数据库存储，通过SQLAlchemy ORM进行数据访问。
系统提供完整的用户管理、新闻浏览、收藏和历史记录功能。

前端通过向后端发送response.post请求，构建请求数据体，从后端接受流式响应
前端将接收到的参数如下，打包成http请求发送到后端，FastAPI 通过 Form 参数接收前端发送的数据：
data = {
    "query": query,              # 从 prompt 提取
    "sys_prompt": sys_prompt,     # 来自 additional_inputs[0]
    "history_len": history_len,    # 来自 additional_inputs[1]
    "history": [str(h) for h in history],  # 历史对话
    "temperature": temperature,    # 来自 additional_inputs[2]
    "max_tokens": max_tokens,      # 来自 additional_inputs[3]
    "session_id": session_id,      # 来自 additional_inputs[4] (注意：stream 参数没发送)
}
# 发送 POST 请求
response = requests.post(chat_with_agent_url, files=files, data=data, stream=True)

## 1-2 技术栈

- **后端框架**: FastAPI
- **数据库**: SQLite
- **ORM**: SQLAlchemy (异步)
- **数据库驱动**: aiomysql
- **密码加密**: passlib + bcrypt （X 未使用）
- **缓存系统**: Redis（X 未使用）
- **异步支持**: Python asyncio

## 1-3 项目结构

```
AI_assiter/                          # 项目根目录
├── chat/                                # 聊天ai助力
│   ├── chat_routes.py                 # 创建聊天路由
│
├── database_server/             # 数据库配置
│   ├── database.py                    # 创建数据库
│   ├── kb_add.py                       # 数据库添加操作
│
├── knowledgebase_server/   # 知识库配置
│   ├── loader.py                        # 文本上传
│   ├── splitter.py                       # 文本分割
│   ├── kb_routes.py                  # 知识库路由
│
├── tools/                              # 工具函数目录
│
├── utils/                               # 
│
├── configs/                          # 配置相关
│   ├── promp.py                      # 提示词工程
│   ├── setting.py                      # 路径参数
│ 
├── app_launch.py                    # 应用入口文件
└── webui1.py                          # 页面配置
```

## 1-4 功能模块

### 1. AI聊天模块

- 消息输入
- 消息发送
- 输入提示词
- 温度参数，历史记录参数调节按钮
- 文件上传功能
- 文件rag检索增强

## 1-5 数据库设计

### 主要数据表

1. **用户表 (user)**
    - 用户基本信息存储
    - 包含用户名、密码(加密)、昵称、头像等字段

2. **用户令牌表 (user_token)**
    - 用户认证令牌管理
    - 支持令牌过期机制

3. **新闻分类表 (news_category)**
    - 新闻分类信息

4. **新闻表 (news)**
    - 新闻内容存储
    - 包含标题、内容、作者、浏览量等字段

5. **收藏表 (favorite)**
    - 用户收藏记录
    - 关联用户和新闻

6. **浏览历史表 (history)**
    - 用户浏览历史记录
    - 关联用户和新闻


## 1-7 API 接口说明

### 聊天接口

| 接口                 | 方法 | 说明         |
| -------------------- | ---- | ------------ |
| `/chat/agent_chat` | POST | 与 Agent 进行对话，支持文件上传、历史记录、参数配置等
     |


### 知识库管理接口
使用在线 Embedding 的路由
| 接口                   | 方法 | 说明             |
| ---------------------- | ---- | ---------------- |
| `/knowledgebase/create_kb` 	      | POST |    创建知识库 |
| `/knowledgebase/delete_kb`       | DELETE | 删除知识库     |
| `/knowledgebase/list_kbs`           | GET  |     列出所有知识库     |
| `/knowledgebase/upload_docs`  | POST |    上传文件并向量化     |
| `/knowledgebase/delete_docs`    | POST |    删除知识库中的文件     |
| `/knowledgebase/search_kb`       | POST |    检索知识库内容     |


## 1-9 错误处理

系统提供统一的错误处理机制：

- 用户认证失败返回 401 状态码
- 资源不存在返回 404 状态码
- 服务器内部错误返回 500 状态码

## 1-10 开发规范

- 使用异步数据库操作
- 所有密码均加密存储
- 接口返回统一的 JSON 格式
- 详细的接口文档和示例
- 缓存操作封装成独立函数便于调用

## 1-11 性能优化

- 使用 Redis 缓存热点数据
- 异步数据库操作提升并发性能
- 合理的数据库索引设计
- 连接池管理减少连接开销

